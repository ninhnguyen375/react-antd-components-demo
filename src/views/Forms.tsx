import * as React from 'react';
import Common from '../layouts/Common';

interface IFormsProps {}

const Forms: React.FunctionComponent<IFormsProps> = props => {
  return <Common>
  hi
  </Common>
};

export default Forms;
